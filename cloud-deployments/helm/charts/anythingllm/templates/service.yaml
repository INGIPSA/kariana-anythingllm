apiVersion: v1
kind: Service
metadata:
  name: {{ include "kariana.fullname" . }}
  labels:
    {{- include "kariana.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kariana.selectorLabels" . | nindent 4 }}
